# 简历事实证据映射

> 用途：投递前核对数字，面试时区分“生产事实”“脱敏重建证据”“外部集成边界”。  
> 最近复核：2026-08-06。

## 项目一、项目二

| 简历主张 | 口径 |
|---|---|
| WAF 约 23 亿、管理台约 10 亿、消息调用约 600 万/日 | 行内生产监控口径；本仓不含原系统代码 |
| Netty 单机万级 QPS | 接收、长度约束与 CRC 校验压测口径；业务处理异步执行 |
| 核心服务可用性 99.99% | 行内生产监控口径 |
| 风控 P99 < 150ms、日均调用 1000 万+ | 行内生产监控口径 |

面试时不要把以上数字说成本仓测试结果；如被追问，应说明统计周期、压测边界和本人负责模块。

## 项目三：纪要君

| 简历主张 | 主要证据 |
|---|---|
| `条线 × 场景`、Skill Pack、L0–L3 | `jiyaojun/app/registry/`、`app/skills_runtime/`、`skills/` |
| Bounded Agent Loop、工具发现、PreToolUse、Observation | `app/agents/bounded_loop.py`、`tool_discovery.py`、`hooks.py` |
| Session Journal、分支恢复、上下文压缩 | `app/memory/session_journal.py`、`context.py`、`repository.py` |
| Continuum、跨会 open items、ACL/write class | `app/knowledge/series.py`、`series_bridge.py`、`plane.py` |
| SOP / Playbook 双路径与 L0 fallback | `app/orchestrator/service.py`、`app/planes/pipeline/step_engine.py`、`sop_executor.py`、`playbook_executor.py` |
| ToolRuntime 唯一副作用出口、HITL、幂等 | `app/tools/runtime.py`、`app/planes/pipeline/work_embed.py` |
| ACL-first Hybrid RAG、结构分块、引用、Faithfulness | `app/knowledge/rag.py`、`chunking.py`、`grounding.py` |
| Hit@5=1.0、MRR=0.9286、nDCG@5=0.9473、Faithfulness=0.932 | `fixtures/eval/rag_golden.yaml`、`python -m app.eval.retrieval_quality` |
| 架构/功能/故事负例/pytest 全绿 | `python -m app.eval.run_all` |
| 20 分钟 → 2 分钟 | 内部试点口径，不是仓库模拟耗时 |

边界：脱敏工程使用确定性 Mock Planner、bge-m3-shim、内存索引、进程内调度器和 Mock/SPI 外部系统；生产真实模型、向量库、消息中间件和工作系统不在仓库内。

## 项目四：大模型安全控制面

| 简历主张 | 主要证据 |
|---|---|
| L1 输入、L2 推理前、L3 输出、L4 ToolRuntime | `docs/llm-safety-platform/DECISIONS.md`、`03_架构基线.md` |
| 五态 SafetyDecision | `llm-safety-platform/app/decisions.py`、`docs/llm-safety-platform/07_SafetyDecision公共信封.md` |
| 规则 + 可选远程 Judge | `app/scanners/`、`app/eval/remote_judge_compare.py` |
| Dual-Gate 与唯一语料指标口径 | `app/eval/dual_gates.py`、`corpus_metrics.py` |
| publish profile、critical 双人审批与最终重评 | `app/eval/publish_profile.py`、`app/api/main.py` |
| 审计哈希链、跨重启恢复、损坏 fail-closed | `app/observability/chain_verify.py`、`app/bootstrap.py`、`app/db/migrate.py` |
| CI 120+310、release 600+410、full 2400+410 | `configs/evals/attack_corpora/reports/dual_gate_*.json` |
| pytest 103 项、run_all 全绿 | `python -m pytest tests -q`、`python -m app.eval.run_all` |

边界：上述 0 漏拦/0 误杀来自脱敏静态语料与 shim，只证明门禁可复现，不代表生产零事故；远程 Judge、garak/PyRIT 无配置时显式跳过；审计链脱敏实现按 single-writer，生产多副本需要 advisory lock 或独立 audit writer。

## 投递口径

1. 默认投递 `个人简历.md`；架构面或高级岗位补充 `个人简历_纪要君架构版.md`。
2. 先讲业务问题、本人决策和结果，再展开模块名。
3. 生产事实与脱敏回归数字分开，不把 Mock/shim 说成线上模型效果。
4. 任何新增数字先补统计周期、样本量或压测边界，再写入简历。
