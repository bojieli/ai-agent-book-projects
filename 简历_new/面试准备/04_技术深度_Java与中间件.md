# 04｜技术深度：Java 基础、JVM、中间件

> 简历写了"扎实"，面试官会逐个拆。本文件只保留**通用原理与高频问法**，所有“项目细节”统一回到：`01/02/03` 三个项目文件，避免重复背诵。

---

## 0. 使用方式（减重版）

- **先背 [必答]**：保证任何面试都不失分
- **再补 [加分]**：只在面试官深挖或你想引导时展开
- **每一块都写 1 个“项目例子”**：用你自己的真实链路/事故来证明（下方每节都给了填空位）

---

## 1. Java 并发编程 / JUC（简历写了"熟悉"，面试必深挖）

- **[必答] ThreadLocal**：线程池场景的坑是什么？为什么必须 `remove`？WeakReference 解决了什么、没解决什么？
- **[必答] AQS**：ReentrantLock vs synchronized 的核心差异；AQS 的 state/队列/独占共享如何映射到实际锁实现？
- **[常问] ConcurrentHashMap**：1.7 分段锁 vs 1.8 CAS+synchronized；扩容 transfer 的并发协助机制。
- **[常问] CompletableFuture**：`thenCompose` vs `thenCombine`；异常如何透传与兜底。
- **[加分] volatile 与 happens-before**：可见性/有序性怎么保证？在“缓存异步刷新/热加载”里如何避免读到半更新状态？

**项目例子填空位（写 3 句就够）**：
- 我在【项目__】里用到并发点是【__】，当时的竞态/可见性风险是【__】；我用【volatile/锁/原子类/单线程模型】解决，并通过【压测/线上指标/复盘】验证。

---

## 2. JVM / GC 调优（"内存池分配策略优化"必须落到 GC 层面）

- **[必答] 线上用哪个 GC？为什么选它？** G1/CMS/ZGC 的取舍点是什么（停顿/吞吐/成熟度/运维成本）？
- **[必答] GC 日志怎么看？** 你关注哪些指标（频率/停顿/晋升失败/混合回收）？如何把现象和参数调整关联起来？
- **[常问] G1 调优会看哪些点？** Pause 目标、Humongous 对象、Mixed GC 触发条件、Region 相关参数你怎么理解？
- **[常问] 你具体改过哪些参数？** 改因→改法→改后指标变化（至少说 1 组前后对比）。
- **[加分] Java 版本选择**：JDK 版本、升级策略与风险控制；虚拟线程对“阻塞 IO/线程模型”的影响你怎么看？

**项目例子填空位**：
- 我遇到的现象是【CPU/RT 抖动/频繁 GC/OOM】；证据来自【GC 日志/Heap Dump/APM】；根因是【__】；我改了【__ 参数/对象分配/缓存策略】后【指标__】改善。

---

## 3. Redis（缓存穿透 / 热 key / 一致性）

- **[必答] 穿透/击穿/雪崩的区别与治理**：你在项目里对应的是哪一种？用的组合拳是什么？
- **[必答] 一致性策略**：旁路缓存/写穿/双写一致性怎么取舍？如何避免失效风暴？
- **[常问] 二级缓存（本地 + Redis）**：TTL 怎么设？一致性怎么保？如何做降级与回源限流？
- **[常问] Redis 高可用形态**：Sentinel vs Cluster；主从切换抖动如何感知与止损？
- **[加分] 热 key 治理**：Key 分片/本地预聚合/批量合并上报/只读副本等，你能说清“为什么有效”和“代价”。

**项目例子填空位**：
- 我在【项目__】的热点数据是【__】，峰值读写特征【__】；我用【二级缓存/Key 分片/本地预聚合】把【延迟/Redis CPU/错误率】压到【__】。

---

## 4. RocketMQ（简历列为核心，但项目描述里覆盖较少——高危）

> 面试官会注意到：项目描述里 RocketMQ 只在项目一背景提到"RocketMQ 调用数据"，并无专项建设，简历却把它列为核心技能。

- **[必答] 你在 RocketMQ 上最深的工作是什么？** 请落到“一个真实问题 + 你的方案 + 指标改善”。
- **[必答] 幂等怎么做？** 幂等键在哪里，去重在哪层做，失败重试如何不放大副作用？
- **[常问] 堆积怎么定位与治理？** 从生产/消费/Broker 三侧分别怎么排？
- **[加分] 顺序与并发取舍**：什么时候需要顺序消息？为什么多数场景不值得？
- **[备选] 23亿日志用 MQ 怎么设计？** Topic/队列数估算、消费组划分、流控与背压。

**项目例子填空位**：
- 我在【项目__】处理过 MQ 的【重复/堆积/延迟/乱序】；关键证据是【__】；我通过【幂等/重试/限流/扩容】把【指标__】从【__】改善到【__】。

---

## 5. MySQL（MVCC / 事务 / 索引）

- **[必答] RR 下的幻读与间隙锁**：请结合一条真实 SQL（可脱敏）讲“为什么会锁、锁什么、怎么规避”。
- **[必答] MVCC 的 ReadView**：快照读 vs 当前读的区别；什么时候会读到最新？
- **[常问] 索引优化**：如何用 explain/慢日志定位；覆盖索引/回表/联合索引顺序如何设计？

**项目例子填空位**：
- 我在【项目__】遇到过【慢查询/锁等待】；SQL 形态是【__】；我通过【索引/SQL 改写/隔离级别】把【耗时/锁等待】从【__】降到【__】。

---

## 6. Netty（已在项目一覆盖，此处补充通用知识点）

- **[必答] Reactor 线程模型**：boss/worker 的职责；线程数怎么估；IO 与业务计算怎么隔离？
- **[常问] ChannelHandler 线程安全**：什么时候可以 `@Sharable`？共享状态怎么处理？
- **[加分] write/flush 的差异**：批量 flush 的意义，如何避免频繁 flush。

> 项目一里的“私有协议/CRC32/压缩/背压/零拷贝”细节请统一以 `01_项目一_数据安全监测中心.md` 为准。

---

## 7. Flink / 实时风控（新版简历新增，高频必问）

- **[必答] 为什么要引入 Flink？解决了什么痛点？**
- **[必答] 事件时间 vs 处理时间**：为什么风控特征更适合事件时间？水位线怎么设？
- **[必答] Checkpoint 与一致性语义**：exactly-once / at-least-once 如何取舍？恢复后如何避免重复输出？
- **[常问] Backpressure 怎么看、怎么治？** 定界 source/算子/sink 哪段慢；如何调并行度/批量/限速。
- **[加分] 状态膨胀治理**：state TTL、key 基数、窗口与迟到策略的取舍。

**项目例子填空位**：
- 我在【项目二】引入 Flink 做【时序特征预计算/链式识别】；关键难点是【乱序/迟到/状态一致性】；我用【事件时间+水位线+Checkpoint】保证正确性，并用【Checkpoint 成功率/延迟/回放对账】验证。

---

## 8. CEP / 链式欺诈识别（项目二简历明确写了）

- **[必答] CEP 模型怎么建？** Pattern、时间窗口、状态约束怎么定？
- **[常问] 命中路径如何解释？** 如何把“多步行为序列”落到可审计的明细里？
- **[常问] 状态膨胀怎么控？** 超时清理、TTL、key 分层/采样。

---

## 9. 配置下发与审计：MySQL CDC / Aviator / ClickHouse

- **[必答] 为什么要 CDC 下发？** 解决“发版依赖/回滚慢/版本不一致”的哪个问题？
- **[必答] 规则版本一致性怎么保证？** 灰度/回滚/生效时间窗口。
- **[常问] ClickHouse 用来做什么？** 为什么适合存命中明细/审计追溯？（宽表、写入与查询模型）

---

## 10. AI 工程化（Python 异步 / 幻觉回归）

- **[必答] Python 异步编程**：并发限制、超时、重试、熔断怎么做？如何避免“重试风暴”？
- **[必答] 幻觉治理的回归集**：样本来源与分层；指标（准确率/误报/漏报/工具成功率）；如何自动化回归。
- **[常问] Java → Python 的工程差异**：依赖管理、类型、并发模型、部署与观测差异，你怎么适配？
- **[常问] 长文本截断与序列修复**：消息序列约束是什么？安全截断策略是什么？

> 项目三里的“意图路由/HITL/状态持久化/工具调用可靠性”细节请以 `03_项目三_纪要君Agent.md` 为准。
