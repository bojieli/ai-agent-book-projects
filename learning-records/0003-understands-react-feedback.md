# 理解 ReAct 循环中的反馈回传

用户已理解：工具结果如果没有追加到消息历史，模型下一轮就无法看到先前行动，可能重复调用搜索工具；搜索结果应先提供标题、链接、来源和日期等信息，再选择性读取正文；搜索排名不能替代证据可靠性，需要比较多个独立来源。这为下一步学习真实 API 的 `messages` 与 `tool_calls` 结构建立了基础。

## Evidence

用户准确判断缺失工具结果会造成重复搜索，并指出“排名第一不代表最可靠，需要多样本”。
