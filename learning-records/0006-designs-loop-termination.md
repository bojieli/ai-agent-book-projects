# 能设计 Agent 循环检测与终止机制

用户已能区分循环检测与硬性终止：通过工具名、标准化参数、结果或错误以及近期调用序列发现直接重复或周期循环；检测后先检查并尝试恢复，持续失败再终止；无论任务是否仍有进展，达到最大工具调用次数都应停止。后续实现 Agent 循环时可以直接引入有限重试和轮数上限。

## Evidence

用户提出比较工具调用结果、分类统计并对比重复调用，给出“先做检查、尝试恢复，再失败才会终止”的处理顺序，并确认达到二十次硬性上限时应该停止。
