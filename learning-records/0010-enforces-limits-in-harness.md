# 能区分提示要求与程序硬约束

用户已理解工具调用上限必须由 Python Harness 直接计数和拦截，不能仅依赖模型遵守提示词；对于一次请求多个工具的边界情况，也应在执行前检查总量。即使拒绝一批工具调用，也要为每个调用 ID 写回对应的未执行结果，保持消息记录完整。这为实现确定性的预算控制、循环终止和安全护栏奠定了基础。

## Evidence

当模型在已经调用二十次工具后仍要求继续时，用户选择由程序强制拦截第二十一次调用；当只剩一次额度而模型同时请求三个工具时，用户选择在执行前拦截，并为三个调用分别写回对应结果。
