# 能读懂工具调用的消息配对

用户已能识别工具结果消息需要包含 `role`、`tool_call_id` 和 `content`，并理解 `assistant.tool_calls` 只是模型的调用请求，工具结果必须通过唯一调用编号与请求一一配对。后续可以进入可运行的 Agent 循环实现。

## Evidence

用户准确说明：“相同的 `tool_call_id` 会导致模型无法区分结果属于哪个工具的调用。”进一步教学中已明确，ID 配对的是具体调用，即使同一个工具调用两次也需要两个不同编号。
